import {
    StyleSheet
} from 'react-native';


export const savedKundalisPageStyles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    text: {
        fontSize: 24,
        fontWeight: 'bold',
        color: '#0000ff',
    },
});