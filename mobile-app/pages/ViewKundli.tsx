import * as React from 'react';
import { View } from 'react-native';

type ViewKundliProps = {
  onNavigateToCreate?: () => void;
};

export default function ViewKundli({ onNavigateToCreate }: ViewKundliProps) {
  return <View />;
}
